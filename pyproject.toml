[tool.poetry]
name = "smart-swarm-logistics"
version = "0.1.0"
description = ""
authors = ["venysssssssssss <viniciussport2004@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.12,<3.15"  # Requires Python 3.12 to 3.14
torch = "^2.10.0"
mesa = "^3.5.0"
paho-mqtt = "^2.1.0"
matplotlib = "^3.10.8"
pytest = "^9.0.2"
numpy = "^2.4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
